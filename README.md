# git-watch
Simple directory watcher for auto push/pull.  

## Usage
```bash
git-watch <path>
```

## Installation
Requires libgit2 to build. Tested with version 1.8
```bash
make
sudo make install
```
